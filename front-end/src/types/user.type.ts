/// TODO User finish type

export type User = {
    email: string;
    firstName: string;
    lastName: string;
    is_admin: boolean;
    is_active: boolean;
    createdAt: string;
    updatedAt: string;
}